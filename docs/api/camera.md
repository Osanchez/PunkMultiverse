# API index — Camera

Generated by `tools/gamescan` from `Punk.Main.dll` (game `steam-24507299`, module `982c5693`).
**Do not edit** — regenerate with `tools/gamescan.ps1 -Index`.
Prose for this area: [`camera.md`](camera.md)

9 types.

## `CameraExtentions`

`class`

<sub>methods</sub>

```csharp
Void Zoom(Camera,Single,Single,Single)
Void Zoom(Camera,Single,Single)
```

## `CameraTargetBase`

`class : MonoBehaviour`

<sub>fields</sub>

```csharp
Color GizmoColor
Vector2 _localPosition
ProCamera2D _proCamera2DInstance
ShipInput _shipInput
Transform _target
Vector2 _targetLocalPosition
Single cameraLockAmount
Single duration
Single inertia
Single maxDistance
ShipInput shipInput
Single targetInfluenceH
Single targetInfluenceV
Vector2 targetOffset
```

<sub>properties</sub>

```csharp
Single MaxDistance
ProCamera2D ProCamera2DInstance
ShipInput ShipInput
Vector2 TargetLocalPosition
```

<sub>methods</sub>

```csharp
Void ctor()
Void Awake()
Boolean IsUsedForInput(ShipInput)
Void LockCamera(Single)
Void OnControlSchemeChanged(ShipInput)
Void OnDestroy()
Void OnDisable()
Void OnDrawGizmos()
Void OnEnable()
Void OnValidate()
Void UnlockCamera(Single)
Void Update()
Single get_MaxDistance()
ProCamera2D get_ProCamera2DInstance()
ShipInput get_ShipInput()
Vector2 get_TargetLocalPosition()
Void set_TargetLocalPosition(Vector2)
```

## `FreeMoveCamera`

`class : MonoBehaviour`

<sub>fields</sub>

```csharp
Camera camrera
InputActionReference cancelAction
InputActionReference fastMoveAction
Single fastMovementSpeed
FastTravelManager fastTravelManager
Single lerpSpeed
InputActionReference moveAction
Single movementSpeed
List`1<GameObject> objectsToDisable
Single originalDistance
ProCamera2D proCamera2D
ShipManager shipManager
Vector3 targetPosition
InputActionReference zoomAction
```

<sub>methods</sub>

```csharp
Void ctor()
Void Awake()
Void OnDisable()
Void OnEnable()
Void Update()
```

## `MouseCameraTarget`

`class : CameraTargetBase`

<sub>fields</sub>

```csharp
Camera _mainCamera
Single screenHeightRatio
```

<sub>properties</sub>

```csharp
Camera MainCamera
```

<sub>methods</sub>

```csharp
Void ctor()
Boolean IsUsedForInput(ShipInput)
Void Update()
Camera get_MainCamera()
```

## `ObjectShaker`

`class : MonoBehaviour`

<sub>fields</sub>

```csharp
Vector2 originalPosition
List`1<ObjectShaker/ShakeAnim> shakes
```

<sub>methods</sub>

```csharp
Void ctor()
Void Shake(Vector2,Single,Single,Single)
Void Start()
Void Update()
```

## `ObjectShaker/ShakeAnim`

`struct : ValueType`

<sub>fields</sub>

```csharp
Single amplitude
Vector2 direction
Single duration
Single frequency
Single startTime
```

<sub>methods</sub>

```csharp
Vector2 GetDeltaPosition()
```

## `OrthoSizeFromFiewOfView`

`class : MonoBehaviour`

<sub>fields</sub>

```csharp
Camera orthoCamera
Camera referenceToMatch
```

<sub>methods</sub>

```csharp
Void ctor()
Void Update()
```

## `POICameraTarget`

`class : MonoBehaviour`

<sub>fields</sub>

```csharp
Single activationDistance
Single duration
Boolean isActive
ProCamera2D proCamera2DInstance
ShipManager shipManager
Single targetInfluenceH
Single targetInfluenceV
Vector2 targetOffset
```

<sub>methods</sub>

```csharp
Void ctor()
Void Awake()
Void OnDisable()
Void Update()
```

## `ShakeOnStart`

`class : MonoBehaviour`

<sub>fields</sub>

```csharp
ShakePreset shakePreset
```

<sub>methods</sub>

```csharp
Void ctor()
Void Start()
```

