bin/
obj/
dist/
*.user
.vs/
punkmultiverse-refs.zip
refs-staging/


# pelican_egg image build stages the BepInEx loader here (binaries, do not commit)
pelican_egg/_bepinex_stage/
pelican_egg/_game_stage/

# Local credentials (SFTP access to the dedicated server, etc.) — never commit
.secret/
